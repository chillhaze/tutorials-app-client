import styled from '@emotion/styled';
import { Box, Typography } from '@mui/material';

export const Wrapper = styled(Box)`
  text-align: center;
`;
export const NoSelected = styled(Typography)`
  margin: 15px 0 30px;
  text-align: center;
  font-weight: 600;
  color: #1976d2;
`;
